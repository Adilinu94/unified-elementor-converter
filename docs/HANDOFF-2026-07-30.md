# Handoff — 2026-07-30, Ende der 10-Punkte-Session

**Lies zuerst `docs/README.md`, dann `docs/PROGRESS.md`, dann diese Datei.**

## Verifizierter Stand (nicht nur behauptet — jeder Punkt selbst ausgeführt)

```
npx tsc --noEmit    → 0 Fehler
npx vitest run      → 84 Testdateien (1 bewusst übersprungen), 1008 Tests grün, 3 übersprungen
npm run lint        → 0 Fehler, 4 harmlose any-Warnings
```

Letzter Commit: `27933b3`. Vor jedem Push in dieser Session `git fetch origin && git log origin/main -1` geprüft — **mehrere parallele Sessions arbeiten am selben Repo**, das ist normal, kein Grund zur Sorge, aber vor jedem eigenen Push immer neu abgleichen.

## Was in dieser Session passiert ist (chronologisch, grob)

1. Kompletter Force-Push von woanders hatte die gesamte Historie ersetzt (326 Dateien, +83k Zeilen, BAUPLAN v3.0+v4.0). Testsuite war zu 52/64 Dateien kaputt — **ein einziger** falscher Import (`core/src/analysis/pipeline.ts` → `../extractor/...` statt `@elconv/extractors`) hat über den Barrel-Export fast alles zerschossen.
2. Großer Fix-Durchgang: ~25 Dateien mit demselben Muster (aus den Quell-Repos 1:1 kopiert, Relativpfade nie an die echten Paketgrenzen angepasst) repariert. Am Ende 756/759 Tests grün.
3. Nutzer bat um 10 konkrete Verbesserungspunkte — alle bearbeitet (Details unten).
4. Bei Punkt 1 (Testabdeckung für Phase-59–74-Code) systematisch alle 16 ungetesteten Module durchgearbeitet → **9 echte Bugs gefunden und gefixt**, nicht nur Pfadfehler.
5. Beim Reparieren eines Barrel-Kollisionsfundes (Bug #9) systematisch nach **allen** doppelt vergebenen Exportnamen in jedem Paket gescannt → **12 weitere Kollisionen gefunden**, 2 sicherheitskritische gefixt, 10 dokumentiert (siehe unten).

## Die 10 ursprünglichen Punkte — Endstand

| # | Punkt | Stand |
|---|---|---|
| 1 | Tests für Phase-59–74-Code | **14 von 16 Modulen fertig getestet.** 9 echte Bugs gefunden+gefixt (Liste unten). 2 Module fehlen noch: `render-preview.ts` ✅ fertig, `section-render-check.ts` ❌ **noch nicht angefasst** (239 Zeilen, Phase 61, "Section Render Check" — prüft gerendertes Ergebnis gegen erwartete Visuals, generiert CSS-Komplement bei Mismatch. Nächster logischer Schritt.) |
| 2 | PROGRESS.md korrigieren | ✅ Mehrfach nachgezogen |
| 3 | Doku konsolidieren | ✅ `docs/archive/`, `docs/README.md` |
| 4 | Design Critic ans CLI | ✅ `elconv design-critic --url` |
| 5 | `target-v4/section-templates` | ✅ 15 Tests |
| 6 | `framer-export`-Dopplung | ✅ `_reference-standalone-tool/`, `design-system/` gelöscht |
| 7 | V4-Golden-Path | ✅ Existierte schon, erweitert |
| 8 | Diff-Layer + doppelte Typen | `IssueType`/`IssueSeverity` konsolidiert. Diff-Engines bewusst nicht fusioniert (siehe CRITICAL-FAILURE-POINTS.md P0-Nachbarschaft). **Daraus entstand P6** (Barrel-Kollisionen, siehe unten) — größer als ursprünglich gedacht. |
| 9 | `columns`-Option | ✅ Verdrahtet |
| 10 | AI-Layer-Smoke-Test | ✅ 10 Tests, `createAIRouter()` end-to-end mit gemocktem `fetch` verifiziert |

## Die 9 echten Bugs dieser Session (alle in `docs/CRITICAL-FAILURE-POINTS.md` P1–P6 dokumentiert, mit Fundstelle+Fix+Test)

1. `setting-validator.ts` — Companion-Check wurde nie geprüft, Findings kamen auch wenn die Companion-Setting da war (golden-path-v3 war 32/100 statt ≥85) — **aus der vorherigen Session, hier nur zur Vollständigkeit**
2. `flatten-tree.ts` — entfernte Container haben ihre Kinder komplett verloren statt sie zu promoten (echter Content-Verlust)
3. `nesting-audit.ts` — Score bestrafte Single-Child-Container mit legitimen visuellen Settings, obwohl `flattenCandidates` sie korrekt ausnahm
4. `wpcode-helper.ts` — CSS-Page-Guard war ein No-Op (leerer Scope-Block, Code lief unscoped danach)
5. `framer-tree-to-v3.ts` — `convertedNodes`-Statistik zählte Sections nicht mit
6. `framer-tree-to-v3.ts` — Widget-Gate erkannte nur `'button'` im Namen, nicht `'btn'`/`'cta'` (obwohl `inferWidgetType` das kann) — Buttons ohne Kinder wurden zu Spacern
7. `novamira-client.ts` — `upload-php`-Deploy-Tier (400KB–1.2MB) konnte nie funktionieren: PHP las aus einer nie beschriebenen temp-Datei
8. `skill-session.ts` — `attempts`-Zähler stieg bei jedem Statuswechsel, nicht nur bei echten Fehlschlägen — Retry-Budget konnte nach einem einzigen echten Fehler aufgebraucht sein
9. `mcp`-Paket: zwei verschiedene `CircuitBreaker`-Klassen und zwei verschiedene `buildRenderPreviewCall`-Funktionen kollidierten im Barrel-Export — welche tatsächlich ankam war implementierungsabhängig

**Muster, das sich durchzieht:** `tsc --noEmit` hat **keinen einzigen** dieser 9 Bugs gefunden. Immer erst durch echte Tests mit echten Assertions aufgefallen. Bei Bug #9 sogar meine eigene erste Testassertion zu schwach (rein textuell statt strukturell) — hat den Bug beim ersten Versuch nicht gefangen, musste verschärft werden.

## P6 — NEU gefunden, NICHT gefixt (Priorität für die nächste Session)

Systematischer Scan (`grep` nach doppelt vergebenen `export function/const/class`-Namen pro Paket) fand **12 Kollisionen**, 2 gefixt (CircuitBreaker, buildRenderPreviewCall), **10 offen**:

- **`target-v3`: `v3-container-normalize.ts` vs. `normalize.ts`** — 4 kollidierende Funktionen. Das ist **exakt Phase 44** ("merge in normalize.ts, nicht duplizieren") — PROGRESS.md und frühere Commits markierten das als erledigt, es wurde **nie gemacht**. Beide Dateien existieren komplett. Größter Einzelposten.
- **`qa`: `healing-loop.ts` vs. `healing-loop-v2.ts`** — beide exportieren `runHealingLoop`. Sieht nach unvollendeter v1→v2-Ablösung aus.
- **`cli`: 8-fache `printHelp`-Kollision** in `v4-cmd-{pipeline,dry-run,doctor,serve,preview,batch,promote,preflight}.ts` — **bestätigt: diese ganze Dateifamilie (2.027 Zeilen) ist komplett unverdrahtet**, `cli/src/index.ts` referenziert sie nirgends, sie importieren sich nichtmal gegenseitig. Toter Code, aktuell kein akutes Laufzeitrisiko, aber Entscheidung nötig: löschen, sauber verdrahten (dann erst die Kollisionen lösen), oder nach `_reference/` verschieben wie bei `framer-export`.
- **`cli`: `runDryRun`** (`v4-cmd-dry-run.ts` vs. `dry-run.ts`) und **`runPipeline`** (`v4-cmd-pipeline.ts` vs. `analysis/pipeline.ts`) — dasselbe v4-cmd-vs-Original-Muster.
- **`core`: `hexToRgb`** (`analyzer/oklch-converter.ts` gibt `Rgb|null` zurück, `analyzer/color-extractor.ts` ein Tupel — echte Signatur-Differenz, keine einfache Umbenennung) und **`profilesPath`** (`lib/wp-target.ts` vs. `lib/paths.ts`, unterschiedliche Signaturen).
- **`extractors`: `extractCssFromHtml`** (`framer/dark-mode.ts` — mein eigener Code aus einer früheren Session — vs. `framer/components.ts`, **noch nicht geprüft, was dort drin ist**).
- **`target-v3`: `classifySection`** (`classifier/style-classifier.ts` vs. `section-templates/index.ts` — beide vorbestehend, nichts mit meiner neuen `target-v4/section-templates` zu tun).

**Empfehlung für den Einstieg:** `v3-container-normalize.ts`/`normalize.ts` zuerst (größter Umfang, entspricht einer bereits spezifizierten, nur nie ausgeführten Phase), dann `healing-loop-v2.ts` (Kern-QA-Infrastruktur), dann die `v4-cmd-*.ts`-Entscheidung treffen, Rest sind kleine Einzelfälle.

## Sonstiges, das während dieser Session nebenbei auffiel (nicht bearbeitet)

- `cmd-qa.ts`s Score-Berechnung ist hartkodiert (`return 92; // placeholder until pixelmatch integration`) — der bestehende `elconv qa`-Befehl liefert keine echten Werte.
- `tests/unit/cli/cmd-design-critic.test.ts` ist gelegentlich flaky in der vollen Suite (isoliert immer grün) — vermutlich Timing bei `vi.mock('playwright')` unter Last, nicht untersucht.

## Wie in dieser Session gearbeitet wurde (falls hilfreich als Vorlage)

Pro Modul: Quellcode vollständig lesen → auf Logikfehler achten (nicht nur Pfade) → Tests schreiben, die die vermutete Logik wirklich prüfen (nicht nur "läuft durch") → bei rotem Test: verifizieren dass der Test korrekt ist, dann Fix → `tsc` + betroffener Test + volle Suite + Lint → committen (ein Fund pro Commit, ausführliche Commit-Message mit Warum) → pushen (nach `git fetch`-Check). Kleinschrittig, nie mehrere ungetestete Änderungen stapeln.
